apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: observability
  namespace: argocd
spec:
  description: Project for monitoring and metrics
  

  sourceRepos:
    - '*'
    
  destinations:
    - server: 'https://kubernetes.default.svc'
      namespace: argocd
      
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-apps
  namespace: argocd
spec:
  project: observability
  source:
    repoURL: 'git@github.com:andresdelgadillo/adn-homelab-gitops.git'
    targetRevision: main
    path: argocd/applications/observability
    directory:
      recurse: true
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
